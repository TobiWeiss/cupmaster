module.exports = {
  root: true,
  extends: [
    // Your existing extends might be here
  ],
  rules: {
    // Disable the exhaustive-deps rule globally
    'react-hooks/exhaustive-deps': 'off'
  }
} 